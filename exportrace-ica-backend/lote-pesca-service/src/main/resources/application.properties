server.port=${PORT:8080}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/exporsan_lotes?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Internal service URL routing in monolithic setup
auth.service.url=${BACKEND_URL:http://localhost:8080}
lote.service.url=${BACKEND_URL:http://localhost:8080}
calidad.service.url=${BACKEND_URL:http://localhost:8080}
certificacion.service.url=${BACKEND_URL:http://localhost:8080}

# JWT Security parameters
jwt.secret=${JWT_SECRET:exporsan-ica-secret-jwt-security-key-2026-v1}
jwt.expiration=${JWT_EXPIRATION:28800000}

spring.webservices.path=/ws
spring.jpa.open-in-view=false
spring.security.user.password=none

# External integration (mocks/production)
decolecta.api.url=https://api.decolecta.com/v1
decolecta.api.token=sk_4441.RLFtFN7GuWekyCtN9kemz3SkcP1tOatt

spring.jackson.time-zone=America/Lima
spring.jackson.date-format=yyyy-MM-dd'T'HH:mm:ss
spring.jackson.serialization.write-dates-as-timestamps=false
